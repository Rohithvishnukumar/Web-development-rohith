const { isUtf8 } = require("buffer");
const fs = require("fs");

fs.writeFile("test_file.txt" , "hello world" ,function(err){                 // writeFile("name of file" , "content" , "call back function")
    if(err == true){
        throw err;
    }
    console.log("success")
} );


fs.readFile("test_file.txt" , "utf8"  , function(err , data){
    if(err == true){
        throw err;
    }
    console.log(data);
})